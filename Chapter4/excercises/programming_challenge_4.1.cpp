//Minimum or Maximum

#include <iostream>

using namespace std;
/*
int main(){
    unsigned first_number, second_number;

    cout<<"Enter Two Numbers."<<endl;

    //prompt to enter first number
    cout<<"Enter the first number: ";
    cin >> first_number;

    cout << first_number << endl;
    if(isdigit(first_number)== false){
        //prompt to enter second number
        cout<< "Enter the second number: ";
        cin >> second_number;

        if(isdigit(second_number) == false){
            if (first_number < second_number)
                cout << "The first number is smaller than the second number."<<endl;
    
            else if(first_number > second_number)
                cout<< "The first number is greater than the second number."<<endl;
        
            else 
                cout<< "The first number is equal to the second number."<<endl;
        }
        else 
            cout<<"You have entered an invalid value on the second value. Please enter a valid value."<<endl;
    }
    else 
        cout<<"You have entered an invalid value on the first value. Please enter a valid value."<<endl;

    return 0;
}*/

int main(){
    unsigned first_number, second_number;

    std::cout << "Enter two Numbers.";
    std::cout << std::endl;

    std::cout << "Enter First Number: ";
    std::cin >> first_number;

    std::cout << "Enter Second Number: ";
    std::cin >> second_number;

    
}